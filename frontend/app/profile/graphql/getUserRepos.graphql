query getUserRepos($access_key: String!) {
  getUserRepos(access_key: $access_key) {
    description
    full_name
    html_url
    repo_id
    name
    owner {
      avatar_url
      login
    }
  }
}
